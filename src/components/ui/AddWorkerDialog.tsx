

interface AddWorkerDialogProps {
    open: boolean;
    onOpenChange: (open: boolean) => void;
}



export function AddWorkerDialog({ open, onOpenChange }: AddWorkerDialogProps) {

    console.log("AddWorkerDialog", open, onOpenChange);



    return (
        <div>

        </div>
    );
}